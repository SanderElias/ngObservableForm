import { ObservableFromDirective } from './observable-from.directive';

describe('ObservableFromDirective', () => {
  it('should create an instance', () => {
    const directive = new ObservableFromDirective();
    expect(directive).toBeTruthy();
  });
});
